"""
Sorted is a function, not a method
"""

chars = 'hi mom'
new_chars = sorted(chars)          # return sorted characters to new variable
print(new_chars)

names = ["Bob", "Carol", "Ted", "Alice"]
sorted_names = sorted(names)       # return sorted list to new variable
print(sorted_names)